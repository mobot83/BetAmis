# Reduced resource footprint for local kind clusters.
prometheus:
  prometheusSpec:
    retention: 24h
    resources:
      requests:
        memory: "200Mi"
        cpu: "50m"
      limits:
        memory: "400Mi"
        cpu: "200m"
