image:
  tag: 4ff9538f
  repository: ghcr.io/mobot83/betamis/league-service
replicaCount: 2

ingress:
  enabled: true
  host: api.betamis.example.com   # replace with real domain
  tls: true
  issuer: letsencrypt-prod
config:
  DB_HOST: "" # set to managed DB host
  KAFKA_BOOTSTRAP_SERVERS: ""
  KAFKA_SCHEMA_REGISTRY_URL: ""
  JWT_PUBLIC_KEY_LOCATION: ""
  JWT_ISSUER: ""
  OTEL_EXPORTER_OTLP_ENDPOINT: ""
# In production, sensitive values must live in an externally-managed Secret
# (e.g. sealed-secrets, external-secrets-operator, Vault).
# Set create: false and point to the pre-existing Secret name.
secret:
  create: false
  existingSecret: betamis-prod-secrets
